[programs.localnet]
deposit = "target/deploy/deposit-keypair.json"
governance = "target/deploy/governance-keypair.json"

[registry]
url = "https://api.devnet.solana.com"

[provider]
cluster = "Localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
